# Data — never committed (Git is not for data + Krisha privacy)
data/

# Models — ignore intermediate ones, add the final one manually later
models/

# Python cache
__pycache__/
*.pyc

# Virtual environment
venv/
.venv/

# Secrets
.env

# macOS system files
.DS_Store